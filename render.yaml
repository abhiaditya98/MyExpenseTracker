# Inside render.yaml
services:
  - type: web
    name: expense-tracker
    runtime: python
    buildCommand: "./build.sh"  # 🟢 FIXED: Points to your script
    startCommand: "python manage.py runserver 0.0.0:8000"
